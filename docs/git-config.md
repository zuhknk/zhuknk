# Git 配置

## 仓库地址

```
git@github.com:zuhknk/zhuknk.git
```

## 本地路径

```
E:\object_miansi\app-review-analyzer
```

## 推送命令

```bash
cd E:\object_miansi\app-review-analyzer
git add -A
git commit -m "描述改动"
git push origin main
```

## 认证方式

SSH 密钥，已配置在 `C:\Users\zuhknk01\.ssh\id_ed25519`，无需密码。